# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json
inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling

  devenv:
    # url: path:./devenv?dir=src/modules
    url: github:andrzejressel/devenv/golang_hardening?dir=src/modules

